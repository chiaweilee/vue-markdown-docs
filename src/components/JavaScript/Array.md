## Array

### .from()

*not support IE*

create a new, `shallow-copied` Array instance from an array-like or iterable object.

```JavaScript
Array.from('foo')
```

```
// result: ['f', 'o', 'o']
```

```JavaScript
```
